services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gestoria
      POSTGRES_PASSWORD: gestoria
      POSTGRES_DB: gestoria
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
